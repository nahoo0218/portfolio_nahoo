// jQueryコード
$(function() {
  // 1. 非表示にするスクロール位置 (ピクセル)
  const hidePosition = 4800; 
  // 2. 表示を開始するスクロール位置 (ピクセル) 👈 新しい定数を追加
  const showPosition = 1000;
  
  // 3. ウィンドウのスクロールイベントを監視
  $(window).on('scroll', function() {
    // 現在のスクロール位置 (縦方向) を取得
    const scrollY = $(window).scrollTop(); 
    
    // 4. スクロール位置の条件分岐
    
    // **表示する条件:** 1000pxを超えており、かつ 2800px以下である
    if (scrollY > showPosition && scrollY <= hidePosition) {
      // 範囲内なら表示クラスを削除 (表示)
      $('#toggleButton').removeClass('is-hidden');
    } else {
      // 範囲外 (1000px以下 OR 2800px超え) なら非表示クラスを追加 (非表示)
      $('#toggleButton').addClass('is-hidden');
    }
  });

});